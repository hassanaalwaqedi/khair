{
  "installCommand": "echo skip",
  "buildCommand": "test -n \"$API_URL\" || (echo 'API_URL must point to the Render API, including /api/v1' && exit 1); git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter && export PATH=$PATH:$HOME/flutter/bin && cd frontend/khair_app && flutter pub get && flutter build web --release --base-href / --dart-define=API_URL=$API_URL",
  "outputDirectory": "frontend/khair_app/build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
